# Project2 测试数据导入说明

## ✅ 已修复的问题

之前的测试数据使用了**中间层级的代码**，现在已修复为使用**最底层的叶子节点代码**：

### 修复前 ❌
- **地区**：使用区县代码（2级）如 `101`, `102`
- **性质**：使用大类代码（1级）如 `1`, `2`, `3`  
- **行业**：使用大类代码（1级）如 `1`, `2`, `3`

### 修复后 ✅
- **地区**：使用街道/乡镇代码（3级）如 `10101`, `10102` - **1461个**
- **性质**：使用子类代码（2级）如 `101`, `102`, `201` - **22个**
- **行业**：使用子类代码（2级）如 `101`, `102`, `201` - **97个**

---

## 🚀 快速导入

### 一键完成（推荐）

```bash
cd /Users/chanwensheng/Documents/大学/UniFile/大四上/卓越工程/code/backend

./setup_and_import_project2.sh
```

这个脚本会自动：
1. ✅ 创建 project2 数据库
2. ✅ 从 project1 复制表结构
3. ✅ 导入测试数据（1200家企业，14400条报表）
4. ✅ 验证数据

**预计耗时**：30秒 - 2分钟

---

## 📊 数据规模

```
┌─────────────────────┬──────────┐
│ 项目                 │ 数量     │
├─────────────────────┼──────────┤
│ 街道/乡镇（可用）    │ 1,461    │
│ 企业性质（可用）     │ 22       │
│ 行业分类（可用）     │ 97       │
├─────────────────────┼──────────┤
│ 企业总数             │ 1,200    │
│ 调查期数             │ 12       │
│ 报表记录             │ 14,400   │
│ 审核记录             │ 28,800   │
└─────────────────────┴──────────┘
```

---

## ⚙️ 配置后端使用 project2

### 步骤1: 修改数据库配置

编辑文件：
```
backend/yunnan-province/src/main/resources/application.yml
```

找到第9行：
```yaml
datasource:
  url: jdbc:mysql://127.0.0.1:3306/project1?serverTimezone=GMT%2B8...
```

修改为：
```yaml
datasource:
  url: jdbc:mysql://127.0.0.1:3306/project2?serverTimezone=GMT%2B8...
```

### 步骤2: 重启后端服务

```bash
cd backend/yunnan-province
mvn spring-boot:run
```

### 步骤3: 测试前端

1. 刷新浏览器页面
2. 进入"数据分析"页面
3. 选择2022年的调查期
4. 查看地区列表

**预期结果**：
```
地区           企业数量    占比
------------- --------  ------
凤翔街道        15       1.25%   ← 显示街道名称！
古城街道        12       1.00%
钟屏街道        11       0.92%
...
```

---

## 🔍 验证数据

导入后执行以下SQL验证：

```sql
USE project2;

-- 查看企业总数
SELECT COUNT(*) FROM enterprise_info;
-- 应该: 1200

-- 查看地区代码（应该是5-6位数字）
SELECT DISTINCT region FROM enterprise_info ORDER BY region LIMIT 20;
-- 应该看到: 10101, 10102, 10103... (街道级别)

-- 查看性质代码（应该是3位数字）
SELECT DISTINCT nature FROM enterprise_info ORDER BY nature;
-- 应该看到: 101, 102, 103, 104, 201, 202, 301...

-- 查看行业代码（应该是3-4位数字）
SELECT DISTINCT industry FROM enterprise_info ORDER BY industry LIMIT 20;
-- 应该看到: 101, 102, 103, 104, 105, 201, 202...
```

---

## 🎯 对比 project1 和 project2

| 项目 | project1（旧） | project2（新） | 差异 |
|------|----------------|----------------|------|
| 地区代码 | 2级（区县） | 3级（街道） | ✅ 更详细 |
| 性质代码 | 混合 | 2级（子类） | ✅ 统一 |
| 行业代码 | 混合 | 2级（子类） | ✅ 统一 |
| 地区解析 | ❌ 部分失败 | ✅ 全部成功 | ✅ 修复 |

---

## 📝 测试账号

```
省级管理员: admin_province / 123456
市级管理员: admin_kunming / 123456
企业账号:   ent_01010010001 / 123456
```

---

## ⚠️ 注意事项

1. **project1 不会被删除**，您的原有数据仍然保留
2. **可以随时切换回 project1**，只需修改配置文件
3. **测试完成后决定是否迁移**到 project1

---

## 🔄 切换回 project1

如果需要切换回原来的数据库：

1. 修改 `application.yml` 改回 `project1`
2. 重启后端服务
3. 完成！

---

## 🗑️ 删除 project2（可选）

如果测试完成，不再需要 project2：

```bash
mysql -u root -p -e "DROP DATABASE project2;"
```

---

## ✅ 成功标志

导入成功后，您应该看到：

1. ✅ 前端地区下拉框显示**街道/乡镇名称**
2. ✅ 数据分析页面的表格显示**完整地区名称**
3. ✅ 没有显示纯数字的地区代码
4. ✅ 所有图表和数据正常显示

---

**祝测试顺利！** 🎉

如有问题，请检查：
- MySQL 服务是否运行
- 配置文件路径是否正确
- 后端服务是否已重启

