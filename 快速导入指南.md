# 🚀 大规模测试数据 - 快速导入指南

## 📦 已生成文件

✅ **test_data_2022_massive.sql** (8.1 MB)
   - 包含 44,400+ 条测试数据
   - 1,200 家企业 × 12个月 = 14,400条报表
   - 覆盖云南省16个地州市

✅ **generate_massive_test_data.py** (Python生成脚本)
   - 可重新生成测试数据
   - 可自定义企业数量和时间范围

✅ **import_test_data.sh** (一键导入脚本)
   - 自动备份现有数据
   - 自动验证导入结果
   - 交互式操作，安全可靠

✅ **MASSIVE_DATA_README.md** (详细说明文档)
   - 数据结构说明
   - 测试场景示例
   - 常见问题解答

---

## ⚡ 快速导入（3种方法）

### 方法 1: 使用一键脚本（最简单，推荐）

```bash
cd /Users/chanwensheng/Documents/大学/UniFile/大四上/卓越工程/code/backend

./import_test_data.sh
```

按提示输入：
- 数据库地址（默认 localhost）
- 数据库端口（默认 3306）
- 数据库名称（默认 project1）
- 用户名（默认 root）
- 密码
- 是否备份（建议选 y）
- 确认导入（选 y）

**预计耗时**: 30秒 - 2分钟

---

### 方法 2: 直接使用 MySQL 命令（最快）

```bash
cd /Users/chanwensheng/Documents/大学/UniFile/大四上/卓越工程/code/backend

mysql -u root -p project1 < test_data_2022_massive.sql
```

输入密码后等待导入完成。

**预计耗时**: 30秒 - 1分钟

---

### 方法 3: 使用 MySQL Workbench（可视化）

1. 打开 MySQL Workbench
2. 连接到数据库
3. 菜单：Server → Data Import
4. 选择 "Import from Self-Contained File"
5. 浏览选择 `test_data_2022_massive.sql`
6. 点击 "Start Import"

**预计耗时**: 1-3分钟

---

## ✅ 导入后验证

执行以下SQL确认数据正确导入：

```sql
-- 快速验证
SELECT 
    (SELECT COUNT(*) FROM enterprise_info) AS 企业数,
    (SELECT COUNT(*) FROM period_info WHERE investigate_time LIKE '2022%') AS 调查期数,
    (SELECT COUNT(*) FROM enterprise_report_info) AS 报表数,
    (SELECT COUNT(*) FROM report_audit_history) AS 审核记录数;
```

**预期结果**:
| 企业数 | 调查期数 | 报表数 | 审核记录数 |
|-------|---------|-------|-----------|
| 1200  | 12      | 14400 | 28800     |

---

## 🧪 快速测试

### 测试 1: 获取调查期列表

打开 Postman:

```
GET http://localhost:8080/api/period/list?page=1&pageSize=20
```

**预期**: 返回12个调查期（2022-01 至 2022-12）

---

### 测试 2: 取样分析 - 查看昆明市企业分布

```
POST http://localhost:8080/api/dataAnalysis/sampling
Content-Type: application/json

{
  "periodIds": [你看到的某个period_id],
  "regions": [5],
  "statuses": [3, 4]
}
```

**预期**: 返回昆明市各区县的企业分布数据

---

### 测试 3: 前端测试

1. 启动前端开发服务器:
```bash
cd /Users/chanwensheng/Documents/大学/UniFile/大四上/卓越工程/code/frontend/yunnan-province-web
npm run dev
```

2. 浏览器访问: `http://localhost:5000`

3. 导航到 "数据分析" 页面

4. 选择调查期，查看图表和数据

**预期**: 
- 调查期下拉框显示12个选项
- 选择后显示饼图和数据表
- 数据正常加载，无错误

---

## 📊 数据规模一览

### 总体统计
```
┌─────────────────┬──────────┐
│ 数据类型         │ 数量     │
├─────────────────┼──────────┤
│ 企业信息         │ 1,200    │
│ 调查期           │ 12       │
│ 报表记录         │ 14,400   │
│ 审核记录         │ 28,800   │
│ 账号信息         │ 1,217    │
├─────────────────┼──────────┤
│ 总数据量         │ 44,400+  │
└─────────────────┴──────────┘
```

### 地区分布（每个地州市75家企业）
```
临沧市  ████████████  75
丽江市  ████████████  75
保山市  ████████████  75
普洱市  ████████████  75
昆明市  ████████████  75
昭通市  ████████████  75
曲靖市  ████████████  75
玉溪市  ████████████  75
文山州  ████████████  75
红河州  ████████████  75
版纳州  ████████████  75
楚雄州  ████████████  75
大理州  ████████████  75
德宏州  ████████████  75
怒江州  ████████████  75
迪庆州  ████████████  75
```

---

## 🔐 测试账号

### 省级管理员
```
用户名: admin_province
密码:   123456
```

### 市级管理员（示例）
```
昆明: admin_kunming / 123456
大理: admin_dali / 123456
丽江: admin_lijiang / 123456
```

### 企业账号（示例）
```
ent_01010001 / 123456
ent_05050101 / 123456
ent_13013001 / 123456
```

---

## 🎯 核心特性

### ✅ 真实性
- 使用真实的云南省地区代码
- 企业名称符合地方特色
- 行业分布合理（旅游、农业、矿业为主）
- 人数变化模拟疫情影响

### ✅ 完整性
- 所有外键关系完整
- 每家企业12个月数据无遗漏
- 完整的审核流程（市级→省级）
- 覆盖所有业务状态

### ✅ 多样性
- 16个地州市全覆盖
- 6种企业性质
- 22个行业分类
- 企业规模 20-800人

### ✅ 可分析性
- 支持多维度分析（地区×性质×行业×时间）
- 时间序列完整（2022全年）
- 数据量充足（14,400条）
- 适合可视化展示

---

## ⚠️ 注意事项

### 导入前
1. ✅ 确保 MySQL 服务已启动
2. ✅ 备份现有数据（如果重要）
3. ✅ 确保有足够的磁盘空间（至少200MB）
4. ✅ 确保数据库用户有足够权限

### 导入中
1. 不要中断导入过程
2. 不要关闭终端窗口
3. 等待导入完成提示

### 导入后
1. 验证数据数量
2. 检查外键约束
3. 测试基本查询
4. 重启后端服务（如果正在运行）

---

## 🔧 常见问题

### Q: 导入报错 "Access denied"
**A**: 检查数据库用户权限，确保有 INSERT、DELETE 权限

### Q: 导入速度很慢
**A**: 
- 使用命令行导入（比GUI快）
- 临时关闭 MySQL 日志
- 检查磁盘IO性能

### Q: 数据数量不对
**A**:
- 检查是否完整执行了SQL
- 查看 MySQL 错误日志
- 重新导入

### Q: 如何清除数据
**A**:
```sql
DELETE FROM report_audit_history;
DELETE FROM enterprise_report_info;
DELETE FROM report_info;
DELETE FROM account_info WHERE username != 'admin_province';
DELETE FROM enterprise_info;
DELETE FROM period_info WHERE investigate_time LIKE '2022%';
```

---

## 📚 进阶使用

### 重新生成数据（自定义参数）

编辑 `generate_massive_test_data.py`:

```python
# 修改企业数量（每个市）
num_enterprises_per_city = 100  # 改为 100

# 修改年份
for month in range(1, 13):
    year = 2023  # 改为 2023年
```

然后运行:
```bash
python3 generate_massive_test_data.py
```

### 分析数据库性能

```sql
-- 查看索引使用情况
SHOW INDEX FROM enterprise_report_info;

-- 分析查询计划
EXPLAIN SELECT * FROM enterprise_report_info 
WHERE period_id = 1 AND status IN (3,4);

-- 查看表大小
SELECT 
    table_name AS 表名,
    ROUND(((data_length + index_length) / 1024 / 1024), 2) AS 大小MB
FROM information_schema.TABLES 
WHERE table_schema = 'project1'
ORDER BY (data_length + index_length) DESC;
```

---

## 🎓 学习建议

使用这套数据，您可以学习：

1. **SQL优化**: 在大数据量下优化查询性能
2. **索引设计**: 理解索引对查询速度的影响
3. **数据分析**: 实现多维度数据分析
4. **缓存策略**: Redis在大数据场景的应用
5. **可视化**: 使用 ECharts 展示分析结果

---

## 📞 需要帮助？

查看详细文档:
```bash
cat MASSIVE_DATA_README.md
```

重新生成数据:
```bash
python3 generate_massive_test_data.py
```

---

**祝您测试顺利！🎉**

---

**版本**: v1.0  
**生成时间**: 2025-11-03  
**数据规模**: 44,400+ 条记录  
**覆盖范围**: 云南省16个地州市

